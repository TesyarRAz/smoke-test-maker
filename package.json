{
  "name": "smoke-test-maker",
  "version": "1.5.0",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "smoke-test-maker": "bin/smoke-test-maker.cjs"
  },
  "dependencies": {
    "commander": "^9.4.1",
    "dotenv": "^16.0.4",
    "html-to-image": "^1.11.13",
    "mongodb": "^7.2.0",
    "mysql2": "^3.2.0",
    "pg": "^8.11.0",
    "puppeteer": "^24.42.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "prepublishOnly": "tsc",
    "release": "npm run build && npm publish",
    "release:dry": "npm run build && npm publish --dry-run",
    "bundle": "node bundle.js"
  },
  "description": "CLI tool to generate smoke tests from .hurl files with DB query integration",
  "keywords": [
    "hurl",
    "smoke-test",
    "api-testing",
    "http"
  ]
}
